name: Release

on:
  workflow_dispatch:

jobs:
  release:
    runs-on: windows-2022

    steps:
      - name: Install GTK4 (Windows)
        run: |
          pipx install gvsbuild
          gvsbuild build --ninja-opts -j2 gtk4 librsvg
